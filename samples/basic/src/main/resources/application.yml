spring.shell.interactive.enabled: false

ssh:
  shell:
    prompt:
      color: cyan
      text: 'basic::>'
    user: user
    password: password
    commands:
      actuator:
        enabled: false
      jmx:
        enabled: false
      datasource:
        enabled: false
      stacktrace:
        enabled: false
      tasks:
        enabled: false
      system:
        enabled: false
      script:
        enabled: false
      manage-sessions:
        enabled: false

logging:
  level:
    com.github.choseongah: debug
